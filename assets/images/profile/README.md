# Profile picture

Upload profile pictures to this folder and set `basics.photoUrl` in `data/resume.json` to the chosen image.

The current profile picture is:

```json
"photoUrl": "assets/images/profile/jaani.png"
```

Square PNG, JPEG, or WebP images work best with the circular profile frame.
